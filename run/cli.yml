version: '3'
services:
  node:
    image: node:13-alpine
    restart: always
    working_dir: ${PROJECT_DIR}
    user: ${CURRENT_UID}
    volumes: 
      - ${PROJECT_DIR}:/app
      - ${PROJECT_DIR}/run/node/.npm:/.npm